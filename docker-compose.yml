version: '3.8'
services:
  db:
    image: postgres:15
    container_name: loyalmark-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: loyalmark_db
    ports:
      - "5433:5432"